services:
  og-suite:
    build:
      context: .
    image: og-suite:0.1.0
    container_name: og-suite
    restart: unless-stopped
    environment:
      OG_SUITE_BIND: 0.0.0.0:8080
      OG_SUITE_STATIC_DIR: /app/public
    ports:
      - "8080:8080"
